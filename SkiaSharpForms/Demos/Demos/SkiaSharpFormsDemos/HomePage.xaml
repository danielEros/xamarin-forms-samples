<?xml version="1.0" encoding="utf-8" ?>
<local:HomeBasePage xmlns="http://xamarin.com/schemas/2014/forms"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:local="clr-namespace:SkiaSharpFormsDemos"
                    x:Class="SkiaSharpFormsDemos.HomePage"
                    Title="SkiaSharp Forms Demos"
                    Padding="10">
    <ScrollView>
        <StackLayout>

            <Label>
                We came to 3 solutions, none of them can do all the functions we need. Please comment/uncomment the parts in FingerPaintSavePage.xaml andFingerPaintSavePage.xaml.cs to check these solutions. Our aim is to be able to zoom and move the bitmap along with the drawings on it, and we should know the tapped postion in the proper event handler
            </Label>

            <TableView Intent="Menu">

                <TableRoot>
                    <TableSection>
                        <TextCell Text="This is what we could get so far."
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type local:Bitmaps.FingerPaintSavePage}" />



                    </TableSection>
                </TableRoot>
            </TableView>

            

        </StackLayout>
    </ScrollView>

</local:HomeBasePage>